version: "3"

volumes:
  mongodb:

services:
  mongodb:
    container_name: mongodb
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: comms
      MONGO_INITDB_ROOT_PASSWORD: StaySafeAndKeepWorking

  frontend:
    container_name: pmt-frontend
    build:
      context: ./frontend/
      dockerfile: Dockerfile-prod
      args:
        NODE_ENV: production
    volumes:
      - ./frontend:/usr/src/app
    environment:
      NODE_ENV: production
    ports:
      - 80:80
    depends_on:
      - backend

  backend:
    container_name: pmt-backend
    restart: on-failure
    build:
      context: ./backend/
      dockerfile: Dockerfile
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules/
    environment:
      API_PORT: $API_PORT
      MONGO_URI: $MONGO_URI
      NODE_ENV: production
    ports:
      - $API_PORT:$API_PORT
    restart: always
    depends_on:
      - mongodb
